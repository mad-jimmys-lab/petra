{
  "name": "stub",
  "version": "1.0.0",
  "description": "vue-front-stub",
  "scripts": {
    "serve": "npm-run-all -p create-mock boot-mock",
    "create-mock": "node ./scripts/merge_mock_json.js",
    "boot-mock": "npx json-server --watch ./mock.json --port 3010"
  },
  "license": "ISC",
  "devDependencies": {
    "json-server": "^1.0.0-alpha.16",
    "npm-run-all": "^4.1.5"
  }
}
